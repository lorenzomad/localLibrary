extends layout 

block content 

    h1= title 
    ul 
        each genre in all_genres_list 
            li  
                a(href=genre.url) #{genre.name}

        else 
            li No genre available